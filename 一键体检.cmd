@echo off
rem ============================================================
rem  vpn-guard 环境对照体检 —— 双击本文件即可，不需要懂命令行
rem
rem  只读：不改任何系统设置，不装任何东西，不需要管理员权限。
rem  产出：桌面上一份已脱敏的对照报告，发不发由你决定。
rem
rem  如果双击后窗口一闪而过，或提示「无法加载文件」：
rem    多半是压缩包没解锁。右键那个 zip → 属性 → 勾选「解除锁定」→ 确定 → 重新解压。
rem ============================================================

rem 切 UTF-8 代码页，否则中文在部分机器上会是乱码
chcp 65001 >nul 2>&1

cd /d "%~dp0"

powershell -NoProfile -ExecutionPolicy Bypass -File "%~dp0field-report.ps1"

echo.
echo ============================================================
echo  按任意键关闭这个窗口。
echo ============================================================
pause >nul
